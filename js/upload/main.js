/**
 * Main JavaScript file for the upload system
 * This initializes all components and manages the overall integration
 */

document.addEventListener('DOMContentLoaded', function() {
    initStepNavigation();

    initAlbumOptions();

    initFileHandlers();
    
    initUploadSubmission();

});